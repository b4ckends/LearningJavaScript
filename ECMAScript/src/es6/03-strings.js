let hello = 'Hello';
let world = 'World';

// Template literals
let word = `${hello} ${world}`;
console.log(word);

// Multi-line strings
let lorem = `this is a string that says 1
this is the continuation of the string that says 2`;
console.log(lorem);